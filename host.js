let ws,room,idx=-1;
function createRoom(){room=Math.random().toString(36).slice(2,7).toUpperCase();document.getElementById('room').textContent='Room: '+room;let u=location.origin+'/';document.getElementById('joinurl').textContent=u+' (room '+room+')';let qr='https://api.qrserver.com/v1/create-qr-code/?size=300x300&data='+encodeURIComponent(u+'?room='+room);document.getElementById('qr').src=qr;document.getElementById('qr').classList.remove('hidden');let p=location.protocol==='https:'?'wss':'ws';ws=new WebSocket(p+'://'+location.host+'/ws?room='+room+'&id=HOST&name=HOST');ws.onmessage=e=>msg(JSON.parse(e.data))}
function msg(m){if(m.type==='state'){count.textContent=m.players;players.innerHTML=m.names.map(x=>'<div class=player>'+x+'</div>').join('')}if(m.type==='question'){idx=m.index;title.textContent='Question '+(idx+1)+' / 30';hq.textContent=m.question}if(m.type==='tick')htimer.textContent=m.seconds;if(m.type==='answers')answers.innerHTML=m.items.map(x=>'<div class=player>'+x.name+' — '+(x.answer===null?'No answer':String.fromCharCode(65+x.answer))+' '+(x.correct?'✅':'')+'</div>').join('')}if(m.type==='leaderboard')alert(m.items.map((x,i)=>(i+1)+'. '+x.name+' — '+x.score).join('\n'))}
function next(){idx++;if(idx>29)idx=29;ws.send(JSON.stringify({type:'start',index:idx}))}function prev(){idx=Math.max(0,idx-1);ws.send(JSON.stringify({type:'start',index:idx}))}function pause(){ws.send(JSON.stringify({type:'pause'}))}function board(){ws.send(JSON.stringify({type:'leaderboard'}))}